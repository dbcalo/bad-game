## What

<!-- One or two sentences. For a new agent: its id and a line on its style. -->

## Checks

- [ ] `npm run check` passes locally (or CI is green)
- [ ] For a new agent: one file under `agents/`, no changes elsewhere
